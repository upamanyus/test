name: Test

on: push

jobs:
  run-script-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install opam
        run: apt install opam

      - name: Run my shell script
        run: bash ./script.sh
